List of Code Changes
-------------------------
-Scene now based off of screen height/width
-optionsVBox,drawingBox, now based off of scene height/width
-added createWD which draws aggregation(White/Blank Diamonds) relationships. 
-added createClassBox which creates a classbox (3 rectangles w/ 3 textareas inside) with use of createBoxes and createTextAreas
	drawBox does not call drawClassBox like drawLine
-drawLine now called createWD
-createBoxOptions now calls createClassBox instead of drawBox


List of Functionality Changes
--------------------------------------------
-Choose Line Type now draws aggregation(white diamonds) relationship lines
-Choose Box Type now draws class boxes on top of outline rectangle. 
-Scene elements /should/ now align properly on all screen types/sizes. 